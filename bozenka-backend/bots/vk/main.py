import asyncio


async def launch_vk_bot_instance() -> None:
    """
    Launches vk bot instance
    for bozenka platform
    :return: Nothing
    """

    # TODO: Create Launch vk bot instance
    while True:
        print("vk bot instance working")
        await asyncio.sleep(1)