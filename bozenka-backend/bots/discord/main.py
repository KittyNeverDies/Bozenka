import asyncio


async def launch_discord_bot_instance() -> None:
    """
    Launches discord bot instance
    for bozenka platform
    :return: Nothing
    """
    # TODO: Create Launch discord bot instance
    while True:
        print("discord bot instance working")
        await asyncio.sleep(1)