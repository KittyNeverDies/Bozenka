import asyncio


async def launch_telegram_bot_instance() -> None:
    """
    Launches telegram bot instance
    for bozenka platform
    :return: Nothing
    """
    while True:
        print("telegram bot instance working")
        await asyncio.sleep(1)
    # TODO: Create Launch telegram bot instance
